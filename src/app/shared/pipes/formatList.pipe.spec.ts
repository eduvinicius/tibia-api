/* tslint:disable:no-unused-variable */

import { FormatListPipe } from './formatList.pipe';

describe('Pipe: FormatListe', () => {
  it('create an instance', () => {
    let pipe = new FormatListPipe();
    expect(pipe).toBeTruthy();
  });
});
